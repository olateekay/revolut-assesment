apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "revolutchart.fullname" . }}-test-connection"
  labels:
    {{- include "revolutchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "revolutchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
